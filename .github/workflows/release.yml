# Release — on a tag `v*`, each OS runner NATIVELY builds its target and
# attaches the deliverable(s) to a GitHub Release. Native per OS because Tauri
# does NOT cross-compile (WebView2 + MSVC on Windows, WebKit on macOS, WebKitGTK
# on Linux). Pre-release is automatic when the tag contains a `-`
# (v0.1.0-beta.1 → pre-release; v0.1.0 → latest).
#
# Linux ships the BARE binary, like Windows: the kit is `Talos` + `catalog/` +
# `bundles/` side by side, no package. It is built with plain `cargo build
# --release` rather than `cargo tauri build`: the front is static (`frontendDist:
# public`, no beforeBuildCommand) and `tauri::generate_context!` embeds it during
# the cargo build itself, so the CLI would only add a bundling step we do not
# want — and `cargo build --release` is exactly what a developer runs locally,
# which is the path that was verified.
#
# NO runtime smoke-test: the binary is now a NATIVE GUI app (Tauri window), not a
# headless server — it won't boot reliably on a display-less runner. The real
# runtime check is a manual smoke-test on a real Windows machine (see the porting
# garde-fous, kept with the project's private notes). CI proves it COMPILES
# and produces installable bundles; it does not prove it runs.
#
# The runner produces UNSIGNED bundles (signing/notarisation is a separate step,
# deliberately deferred). windows-latest is x64 native → MSVC + WebView2 already
# present, which sidesteps the Parallels cross-compile pitfalls.

name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write # needed to create the Release and upload assets

jobs:
  build:
    strategy:
      fail-fast: false # one OS failing shouldn't cancel the other's artifact
      matrix:
        include:
          - os: macos-14 # Apple Silicon runner → aarch64-apple-darwin
            # the .app (a directory) zipped — a Release asset must be a single file
            artifacts: |
              Talos-macos-aarch64.app.zip
          - os: windows-latest # x64 runner → x86_64-pc-windows-msvc
            # bare exe only — the deliverable is exe + bundles/ rsync'd, no installer
            artifacts: |
              target/release/Talos.exe
          - os: ubuntu-22.04 # x64 runner → x86_64-unknown-linux-gnu
            # bare binary, renamed so the asset says which OS/arch it is for
            artifacts: |
              Talos-linux-x86_64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v5

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      # Same list as ci.yml, for the same reason: tauri's build script links
      # WebKitGTK at compile time, so without the dev libs even `cargo build` aborts.
      - name: System deps for tauri-build (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libsoup-3.0-dev

      # NO rust-cache here: release runs only on tags, and Actions caches are
      # scoped by ref — a tag build can only read caches saved on the SAME tag
      # (or the default branch), never a sibling tag's. So every release started
      # from an empty cache ("No cache found") regardless. Dropping it removes a
      # step that always missed rather than pretending it helps.

      # tauri-cli as a PRECOMPILED binary, not `cargo install` (which recompiled
      # it from source every release — ~6 min on macOS, more on Windows MSVC, and
      # the ref-scoped cache never helped a tag build anyway). tauri-cli is NOT in
      # taiki-e/install-action's native tool list, so we install cargo-binstall
      # (which IS native) and let it fetch tauri-cli's official prebuilt binary
      # from the tauri-apps GitHub Releases (per-platform assets exist).
      - name: Install cargo-binstall
        if: runner.os != 'Linux'
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-binstall

      - name: Install Tauri CLI (prebuilt)
        if: runner.os != 'Linux'
        run: cargo binstall --no-confirm tauri-cli

      # Native GUI build → produces the OS bundle(s). No cross-compile.
      - name: Build (cargo tauri build)
        if: runner.os != 'Linux'
        run: cargo tauri build

      # See the header: the bare binary is the deliverable, and this is the
      # verified local path. Renamed so the Release asset names its OS/arch.
      - name: Build (cargo build --release, Linux)
        if: runner.os == 'Linux'
        run: |
          cargo build --release --bin Talos
          cp target/release/Talos Talos-linux-x86_64

      # A .app is a directory; a Release asset must be a single file. ditto keeps
      # the bundle's symlinks/permissions intact (plain zip would mangle them).
      - name: Zip the .app (macOS)
        if: runner.os == 'macOS'
        run: ditto -c -k --keepParent target/release/bundle/macos/Talos.app Talos-macos-aarch64.app.zip

      - name: Upload bundle(s) to the Release
        uses: softprops/action-gh-release@v3
        with:
          # A tag containing "-" (v0.1.0-beta.1) is flagged pre-release.
          prerelease: ${{ contains(github.ref_name, '-') }}
          files: ${{ matrix.artifacts }}
          fail_on_unmatched_files: false # bundle naming varies by version; don't hard-fail
