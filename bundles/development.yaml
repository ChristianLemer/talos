# Development — the full kit: version control + terminal editing + AI manuals.
# Needs Data (→ Documents → Base): activating Development cascades the whole
# chain (spec §23).
bundle: Development
emoji: 🛠️
usage: Ship code end to end — version control, terminal editing, full tooling.
highlights: [jj, Helix, nushell-dev]
description: Version control and terminal editing on top of the data stack.
needs: [Data]
packages:
  - jj
  - jj skills
  - Helix
  - nushell-dev
