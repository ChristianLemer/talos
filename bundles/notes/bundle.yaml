# Notes — a local Markdown knowledge base. Shipped in the coque, selectable,
# recommended (opt-out): installed by default, but you may decline.

bundle: Notes
emoji: 📓
description: A local Markdown knowledge base.
priority: 30
selectable: true
posture: opt-out

packages:
  - name: Obsidian
    description: Local Markdown knowledge base
    winget: Obsidian.Obsidian
    brew: obsidian
    # NO detect: — Obsidian is a GUI app with no CLI binary on PATH. Presence is
    # derived from the native system-manager ROUTE (winget list on Windows, brew
    # list on Mac), not a hand-written command. Indeterminate only where no system
    # manager is practicable, never absent. See detect.ts systemProbe.
