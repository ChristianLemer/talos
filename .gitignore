node_modules/
panel.log
package-lock.json
# Runtime install history (per machine/user) — never code, never committed.
logs/
# Compiled binaries (deno compile output) + its self-extraction dir.
dist/
talos.log
# Build stamp — generated by scripts/gen-build-info.ts at every build, never committed.
src/build-info.ts
# Rust / Tauri build artefacts (was spike-tauri/.gitignore before the promotion).
/target
/gen/schemas
# Idem pour le spike jetable resté à côté (schémas régénérés par cargo tauri).
spike-tauri/target
spike-tauri/gen/schemas
# Local scratch — anything with a leading underscore (drafts, spikes, logo work).
_*
.playwright-mcp/
# macOS cruft
.DS_Store

# Field notes — describe corporate estates, never committed.
docs/
# Rust build artefacts (this era predates Rust; needed so jj can snapshot).
/target
