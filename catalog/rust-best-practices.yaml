# rust-best-practices — AI manual (skill) for idiomatic Rust: ownership, clippy,
# error handling, testing. From Apollo GraphQL. Pairs with a Rust toolchain.
name: Rust best practices
description: AI manual for idiomatic Rust — ownership, clippy, error handling, testing
skill: apollographql/skills@rust-best-practices
# `npx skills remove/update` take the skill's own name, not our display name
# (which has spaces) — pin it so uninstall/upgrade resolve.
skillName: rust-best-practices
# Presence = the skill dir under ~/.claude/skills.
detect: rust-best-practices
category: [automation]
requires:
  - Claude Code
