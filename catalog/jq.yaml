# jq — the JSON command line. Some plugin hooks call it; an agent reaches for it
# constantly. Small, and everywhere.
name: jq
description: JSON on the command line — plugin hooks and agents both reach for it
winget: jqlang.jq
brew: jq
detect: jq --version
version: latest
category: [foundation]
