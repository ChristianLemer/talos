# 7-Zip — the archiver Windows lacks. Windows-only ON PURPOSE, as the example of a row
# that requires the OS package: macOS has archives built in, so the row leaves the
# perimeter there rather than installing something nobody asked for.
name: 7-Zip
description: The archiver Windows does not ship
winget: 7zip.7zip
version: latest
category: [foundation]
uac: true
requires:
  - Windows
